<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.inmobi.com.directme" >

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".HomePage"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".Splash"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SearchEvent"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".SearchRestaurant"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".VegSearch"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".NonVegSearch"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".StarOutletSearch"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".PubSearch"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ApiRequest"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".RestaurantSearchResult"
            android:label="@string/app_name">
        </activity>

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDyiNoDWj3y0Z7nqvQjbQu9yYRxx_AUdAM" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

</manifest>
